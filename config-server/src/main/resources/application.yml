spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pablotsky321/hotelapp
          default-label: main
          search-paths: config-data
server:
  port: 8888
